apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: reseller
spec:
  rules:
  - host: reseller.fido.gracey.dev
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: reseller
            port:
              number: 8080