apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: rv
spec:
  rules:
  - host: rv.fido.gracey.dev
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: rv
            port:
              number: 8080