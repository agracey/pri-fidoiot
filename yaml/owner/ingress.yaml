apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: owner
spec:
  rules:
  - host: owner.fido.gracey.dev
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: owner
            port:
              number: 8080